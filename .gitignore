node_modules/
dist/
*.local
.DS_Store

# Visual-regression test artifacts (playwright.config.ts's `outputDir`/
# `snapshotDir` failure output) — the committed baselines themselves live
# under tests/visual/**/*-snapshots/ and are tracked normally.
test-results/
playwright-report/
blob-report/
