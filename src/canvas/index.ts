/**
 * Barrel: importing this module registers the `<wuik-viewport>` custom
 * element (self-registers via `customElements.define` on import) and
 * re-exports its class for consumers that want the type.
 */
export { WuikViewportElement } from "./viewport.ts";
